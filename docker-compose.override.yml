services:
  backend:
    ports:
    - 8801:8000
    environment:
      ISOLA_BRIDGE_SECRET: ${ISOLA_BRIDGE_SECRET:-}
      ISOLA_BRIDGE_V2_CUSTOMER_AGENTS: ${ISOLA_BRIDGE_V2_CUSTOMER_AGENTS:-}
      ISOLA_BRIDGE_V2_RETENTION_HOURS: ${ISOLA_BRIDGE_V2_RETENTION_HOURS:-24}
